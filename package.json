{
  "name": "tp_3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: Should run test with command npm run testpush\" && exit 1",
    "testpush": "git add . && git commit -m \"TDD iteration\" && git push && jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.2.0"
  }
}
